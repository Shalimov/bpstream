{
  "name": "bpstream",
  "version": "1.0.0",
  "description": "Back-pressure streaming",
  "main": "./dist/index.js",
  "scripts": {
    "clean:dist": "rm -rf ./dist",
    "build": "npm run clean:dist && npx tsc -p ./",
    "lint:type": "npx tsc -p ./ --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^14.14.10",
    "ts-node": "^9.1.0",
    "typescript": "^4.1.2"
  }
}
